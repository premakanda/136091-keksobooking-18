'use strict';

(function () {

  //  var housingType = document.querySelector('#housing-type');

  // var INPUT_VALUE = 'any';

  // var offers = [];

  // var typeInput = function (data) {
  //   return getFilteringOffer(housingType, data.offers.type);
  // };

  // var getFilteringOffer = function (input, data) {
  //   return input.value === INPUT_VALUE ? true : input.value === data.toString();
  // };

  // var getFilteringData = function () {
  //   var filteredPins = offers.filter(typeInput.types)
  // };

  // var onSuccess = function (data) {
  //   offers = data;
  //   getFilteringData();

  // };

  //  // Обработка событий
  //  housingType.addEventListener('input', function (evt) {
  //   typeInput(evt, 'type');
  // });


})();
